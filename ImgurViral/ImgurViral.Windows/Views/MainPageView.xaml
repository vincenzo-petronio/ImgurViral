<Page
    x:Class="ImgurViral.Views.MainPageView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ImgurViral.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Name="FlipViewTemplate">
            <StackPanel>
                <TextBlock Text="{Binding Description}" />
                <Image Source="{Binding Link}" Stretch="Uniform"/>
            </StackPanel>
        </DataTemplate>
    </Page.Resources>
    
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="#FF89C624">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="1" x:Uid="AppTitle" Text="" FontSize="{ThemeResource FlyoutThemeMinHeight}" VerticalAlignment="Bottom"/>
            <StackPanel Orientation="Horizontal" Grid.Column="2" Margin="0,0,20,0">
                <ProgressRing Grid.Column="2" IsActive="{Binding ProgressRingIsActive}" Height="60" Width="60"/>
                <TextBlock Margin="20,0,0,0" Text="download in progress..." VerticalAlignment="Center" FontSize="48" FontFamily="Segoe UI"/>
            </StackPanel>
        </Grid>
        
        <FlipView Grid.Row="1" VerticalAlignment="Stretch" x:Name="Items" ItemTemplate="{StaticResource FlipViewTemplate}"/>

    </Grid>
</Page>
